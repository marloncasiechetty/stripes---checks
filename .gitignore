# Claude Design canvas working files (not referenced by the site)
crop*.png
# .image-slots.state.json holds live crop/image data — tracked on purpose
.thumbnail

# OS / editor cruft
Thumbs.db
.DS_Store
.vercel/
node_modules/
